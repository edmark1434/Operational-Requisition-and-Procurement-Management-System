[supervisord]
nodaemon=true

[program:php-fpm]
command=/usr/local/sbin/php-fpm
autorestart=true
priority=10
user=root

[program:nginx]
command=/usr/sbin/nginx -g "daemon off;"
autorestart=true
priority=20
user=root
